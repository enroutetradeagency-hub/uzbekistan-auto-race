import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "uz.manus.uzbekistanautorace",
  appName: "UZBEKISTAN AUTO RACE",
  webDir: "dist/public",
  android: {
    allowMixedContent: false,
  },
};

export default config;
