/**
 * Asfalt Atlas design reminder: React is only the dark, unobtrusive frame;
 * the full-screen Babylon road scene remains the visual focal point.
 */
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
